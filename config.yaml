paths:
  data_dir: "data"
  input_dir: "input"
  temp_dir: "temp"
  source_dir: "src"
  model_dir: "model"
  best_model: best_model.keras"
  
params:
  batch_size: 64
  epochs: 5
  patience: 50

data:
  add_derivatives: True
  test_size: 0.15
  val_size: 0.15
  normalization: "minmax"
  window_size: 360
  analytics: False
  dataset_name: "dataset.npz"
